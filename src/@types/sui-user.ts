export type SuiUser = {
    id?: number,
    walletAddress?: string,
    email?: string,
    avatarUrl?: string
    fullName?: string,
    about?: string,
}

export type UserSocialInfo = {
    name: string,
    link: string
}